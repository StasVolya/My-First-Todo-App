import React from "react";

const TodoList = () => {

  const list = {
    title: '',
    id: Math.random()
  }

  return (
    <div className="list-wrapper">
      {}
    </div>
  )
}

export default TodoList